enum Direction { forward, back, left, right, up, down }

enum FlipDirection { front, back, left, right }

enum VideoStream { on, off }

enum RotationalDirection { clockwise, counter_clockwise }

enum Mission { m1, m2, m3, m4, m5, m6, m7, m8 }

enum MissionPadDetection { on, off }

enum MissionPadDetectionDirection { downward, forward, both }
